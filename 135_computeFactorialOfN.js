function computeFactorialOfN(n) {
  // let nums = [];
  // for (var i  = 1; i <= n; i++) {
  //   nums.push(i)
  // }
  // return nums.reduce((a, b) => a * b);

  // OR

  // var factorial = 1;
  // for (var i = n; i > 1; i--) {
  //   factorial *= i;
  // }
  // return factorial;

  // OR

  // let nums = [n];
  // while (nums[0] > 1) {
  //   nums.unshift(nums[0] - 1);
  // }
  // return nums.reduce((a, b) => a * b);
}
